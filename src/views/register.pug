extends layout.pug

block content
    div.auth
            div.form
                h2= 'Register'
                form(action="/register", method="post")
                    include partials/messages
                    if success
                        div.alert.alert-success(role="alert") User Registration Successful

                    else
                        div.form-group
                            label(for="email") Email Address
                            input#username.form-control(type="email", name="email", value=`${oldValues ? (oldValues.email || '') : ''}` required)

                        div.form-group
                            label(for="firstName") First Name
                            input#firstName.form-control(type="text", name="firstName", value=`${oldValues ? (oldValues.firstName || '') : ''}` required)   

                        div.form-group
                            label(for="lastName") Last Name
                            input#lastName.form-control(type="text", name="lastName", value=`${oldValues ? (oldValues.lastName || '') : ''}` required)         
                            
                        div.form-group
                            label(for="password") Password
                            input#password.form-control(type="password", name="password", value=`${oldValues ? (oldValues.password || '') : ''}` required)

                        div.form-group
                            label(for="passwordConfirm") Confirm Password
                            input#password.form-control(type="password", name="passwordConfirm", value=`${oldValues ? (oldValues.passwordConfirm || '') : ''}` required)    

                        button.btn.btn-primary(type="submit") Submit   

                    p.register-now
                        a(href="/login") Login    